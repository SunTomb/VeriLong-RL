# Pilot results

This directory stores locally generated smoke-test outputs for the VeriLong-RL Phase 1 pilot loop.

The oracle and corrupted baselines are parser/metrics sanity checks, not experimental model results.
