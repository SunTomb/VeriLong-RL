# Phase 1 Pilot Data

Generated pilot JSONL files for the VeriLong-RL synthetic multi-document evidence benchmark are written here.

Use `python scripts/generate_pilot.py --config configs/pilot.yaml` to create `tasks.jsonl`, then `python scripts/validate_pilot.py data/pilot/tasks.jsonl` to validate it.
